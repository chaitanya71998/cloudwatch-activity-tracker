# AWS CloudWatch RUM Configuration
# Replace these values with your actual AWS CloudWatch RUM configuration

# Your CloudWatch RUM Application ID (created in AWS Console)
VITE_CLOUDWATCH_APPLICATION_ID=your-rum-application-id

# Your application version
VITE_CLOUDWATCH_APPLICATION_VERSION=1.0.0

# AWS Region where your RUM application is deployed
VITE_CLOUDWATCH_REGION=us-east-1

# Cognito Identity Pool ID for authentication
VITE_CLOUDWATCH_IDENTITY_POOL_ID=us-east-1:your-identity-pool-id

# CloudWatch RUM endpoint (region-specific)
VITE_CLOUDWATCH_ENDPOINT=https://dataplane.rum.us-east-1.amazonaws.com

# Optional: CloudWatch Logs configuration (if using custom logging)
VITE_CLOUDWATCH_LOG_GROUP=your-log-group-name
VITE_CLOUDWATCH_LOG_STREAM=your-log-stream-name

